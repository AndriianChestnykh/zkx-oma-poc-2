/**
 * Wallet connection button
 * Wraps RainbowKit's ConnectButton with app styling
 */
'use client';

import { ConnectButton } from '@rainbow-me/rainbowkit';

export function WalletButton() {
  return <ConnectButton />;
}